---
import { buildLegacyRedirectUrl, getLegacyLocaleFromUrl } from '../utils/legacyRedirect';
import { getLocalizedAboutUrl } from '../utils/routing';

return Astro.redirect(
  buildLegacyRedirectUrl(Astro.url, getLocalizedAboutUrl(getLegacyLocaleFromUrl(Astro.url))),
  301
);
---
