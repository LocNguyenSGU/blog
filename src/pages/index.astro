---
import { buildLegacyRedirectUrl, getLegacyLocaleFromUrl } from '../utils/legacyRedirect';
import { getLocalizedHomeUrl } from '../utils/routing';

return Astro.redirect(
  buildLegacyRedirectUrl(Astro.url, getLocalizedHomeUrl(getLegacyLocaleFromUrl(Astro.url))),
  301
);
---
