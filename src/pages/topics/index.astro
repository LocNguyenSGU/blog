---
import { buildLegacyRedirectUrl, getLegacyLocaleFromUrl } from '../../utils/legacyRedirect';
import { getLocalizedTopicsUrl } from '../../utils/routing';

return Astro.redirect(
  buildLegacyRedirectUrl(Astro.url, getLocalizedTopicsUrl(getLegacyLocaleFromUrl(Astro.url))),
  301
);
---
