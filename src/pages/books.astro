---
import { buildLegacyRedirectUrl, getLegacyLocaleFromUrl } from '../utils/legacyRedirect';
import { getLocalizedBooksUrl } from '../utils/routing';

return Astro.redirect(
  buildLegacyRedirectUrl(Astro.url, getLocalizedBooksUrl(getLegacyLocaleFromUrl(Astro.url))),
  301
);
---
