---
import { buildLegacyRedirectUrl, getLegacyLocaleFromUrl } from '../../utils/legacyRedirect';
import { getLocalizedCategoriesUrl } from '../../utils/routing';

return Astro.redirect(
  buildLegacyRedirectUrl(Astro.url, getLocalizedCategoriesUrl(getLegacyLocaleFromUrl(Astro.url))),
  301
);
---
